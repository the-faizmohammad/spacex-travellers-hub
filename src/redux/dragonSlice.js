import { createSlice } from '@reduxjs/toolkit';

export const dragonSlice = createSlice({
    name: 'dragon',
    initialState: {
        data: [],
        loading: false,
        error: null,
    },
    reducers: {
        // We will Add  reducers
    },
})